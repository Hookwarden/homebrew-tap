name: Formula audit
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  audit:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v6
      - name: brew audit --strict
        run: brew audit --strict --formula Formula/hookwarden.rb
